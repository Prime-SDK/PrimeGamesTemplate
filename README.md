# PrimeGames WebGL Template

Unity WebGL template for PrimeSDK projects.

Install it from the PrimeSDK Package Manager. The template is copied to:

```text
Assets/WebGLTemplates/PrimeGames
```

After installation, select it in Unity WebGL Player Settings.
